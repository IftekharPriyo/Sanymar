# React UI reference

- Use strict typed IPC payloads and small components/hooks.
- Keep business and orchestration logic outside components.
- Maintain visible mock labeling and accessible status/error text.
- Prefer local state and plain CSS until complexity proves otherwise.
- Add Vitest coverage for view logic and service fallbacks.
